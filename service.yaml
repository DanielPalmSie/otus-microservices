apiVersion: v1
kind: Service
metadata:
  name: health-check-service
  labels:
    app: health-check
spec:
  selector:
    app: health-check
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
